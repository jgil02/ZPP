<UserControl x:Class="CarRentalApp.Views.AddClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CarRentalApp.Converters"
             Background="Transparent" Cursor="">

    <UserControl.Resources>
        
        <conv:PasswordComparisonConverter x:Key="PasswordConverter"/>
    </UserControl.Resources>

    <Grid>
        <Border Background="White" CornerRadius="15" Margin="10" Padding="40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    
                    <TextBlock Text="Dodanie nowego klienta" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                    <TextBlock Text="Wypełnij wszystkie dane, aby zarejestrować klienta w systemie." Foreground="Gray" Margin="0,0,0,40"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- KOLUMNA 1 -->
                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="DANE LOGOWANIA" FontSize="12" FontWeight="Bold" Foreground="#922B21" Margin="0,0,0,20"/>

                            <TextBlock Text="Nazwa użytkownika (Login)" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                            <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15">
                                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="40" VerticalContentAlignment="Center" Padding="10,0" BorderThickness="0"/>
                            </Border>

                            <TextBlock Text="Hasło" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                            <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15">
                                <PasswordBox x:Name="Pass1" Height="40" VerticalContentAlignment="Center" Padding="10,0" BorderThickness="0"/>
                            </Border>

                            <TextBlock Text="Powtórz hasło" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                            <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,30">
                                <PasswordBox x:Name="Pass2" Height="40" VerticalContentAlignment="Center" Padding="10,0" BorderThickness="0"/>
                            </Border>

                            <TextBlock Text="DANE OSOBOWE" FontSize="12" FontWeight="Bold" Foreground="#922B21" Margin="0,0,0,20"/>
                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,10,15">
                                    <TextBlock Text="Imię" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5">
                                        <TextBox Text="{Binding FirstName}" Height="40" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Nazwisko" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5">
                                        <TextBox Text="{Binding LastName}" Height="40" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>

                        <!-- KOLUMNA 2 -->
                        <StackPanel Grid.Column="1" Margin="20,0,0,0">
                            <TextBlock Text="DANE ADRESOWE I KONTAKT" FontSize="12" FontWeight="Bold" Foreground="#922B21" Margin="0,0,0,20"/>

                            <TextBlock Text="Email" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                            <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15">
                                <TextBox Text="{Binding Email}" Height="40" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
                            </Border>

                            <TextBlock Text="Ulica" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                            <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15">
                                <TextBox Text="{Binding Street}" Height="40" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
                            </Border>

                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,10,15">
                                    <TextBlock Text="Nr domu" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5">
                                        <TextBox Text="{Binding HouseNumber}" Height="40" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Kod pocztowy" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5">
                                        <TextBox Text="{Binding PostalCode}" Height="40" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </UniformGrid>

                            <TextBlock Text="Miasto" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                            <Border BorderBrush="#D1D8E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,40">
                                <TextBox Text="{Binding City}" Height="40" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
                            </Border>

                            
                            <Button Content="ZAPISZ KLIENTA" 
                                    Height="55" Background="#922B21" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                                <Button.CommandParameter>
                                    <MultiBinding Converter="{StaticResource PasswordConverter}">
                                        <Binding ElementName="Pass1"/>
                                        <Binding ElementName="Pass2"/>
                                    </MultiBinding>
                                </Button.CommandParameter>
                                <Button.Command>
                                    <Binding Path="SaveClientCommand"/>
                                </Button.Command>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#A93226"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>